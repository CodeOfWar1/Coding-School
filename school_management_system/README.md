# school_management_system (React Config)

This folder has been converted from Django runtime files to frontend configuration modules.

- `app.settings.js`: global app settings (name, timezone, theme)
- `app.routes.js`: route + role menu definitions
- `index.js`: barrel export

The old Python files were backed up to:
`legacy-django/school_management_system_original`
